public class Pilot extends Player{
	private int[] coordinates;
	
	public Pilot(int[] coords){
		coordinates = coords;
		
	}
	
	public void giveTreasureCard(Player p, TreasureCard tc) {
		p.addTreasureCard(tc);
		this.getCards().remove(tc);
	}
	
}
