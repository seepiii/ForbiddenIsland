for (Tile tempTile : tiles)
        {

            g.setColor(Color.BLACK);
            g.drawImage(tempTile.getImage(), tempTile.getX(), tempTile.getY(), (int) (259 * scale), (int) (259 * scale), null);
            
        }
